// number.js
// From "Try It!" section: an example of a Readable stream
// It generates a random number, to be provided to a Transform stream

var stream = require('stream');

function NumberGen(n) {
    // n is # of random numbers generated
}

module.exports = NumberGen;